import { CarrierTypes } from '@/types/carrier';

const getQuickShipperTaxAmount = async (params: CarrierTypes.ICarrierTaxParams): Promise<number> => {
  // TODO: QuickShipper duties & taxes API entegrasyonu yapılacak.

  return 0;
};

export default getQuickShipperTaxAmount;
