import { CarrierTypes } from '@/types/carrier';

const getUpsTaxAmount = async (params: CarrierTypes.ICarrierTaxParams): Promise<number> => {
  // TODO: UPS duties & taxes API entegrasyonu yapılacak.

  return 0;
};

export default getUpsTaxAmount;
